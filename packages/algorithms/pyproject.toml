[project]
name = "gtm-advisor-algorithms"
version = "0.1.0"
description = "Analytical layer algorithms for GTM Advisor (scoring, clustering)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "structlog>=24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
