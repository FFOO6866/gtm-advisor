[project]
name = "gtm-advisor-tools"
version = "0.1.0"
description = "Operational layer tools for GTM Advisor (enrichment, scraping, CRM)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
    "structlog>=24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
