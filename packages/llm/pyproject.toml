[project]
name = "gtm-advisor-llm"
version = "0.1.0"
description = "LLM provider abstraction for GTM Advisor"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.30",
    "instructor>=1.0",
    "structlog>=24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
