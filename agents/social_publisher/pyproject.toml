[project]
name = "gtm-social-publisher"
version = "0.1.0"
description = "Social Publisher Agent — publishes approved creative assets to social media platforms"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src"]
