[project]
name = "gtm-strategy-proposer"
version = "0.1.0"
description = "Strategy Proposer Agent — reads insights and proposes high-level strategies for user approval"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src"]
